# examen_Methodo_applique
- lancer le docker first: docker compose up --build

- installer le lucide react 
cmd :
docker compose exec web npm install lucide-react

-installer framer-motion pour les animations
docker compose exec web npm install framer-motion